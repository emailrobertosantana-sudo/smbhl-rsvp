{
  "name": "smbhl-rsvp-tmp",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "schema:manifest": "node scripts/generate_schema_manifest.js",
    "schema:check:demo": "node scripts/check_schema_remote.js notreligue-demo --remote --env demo",
    "schema:check:production": "node scripts/check_schema_remote.js smbhl-rsvp --remote"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.0.0",
    "acorn": "^8.18.0",
    "vitest": "~4.1.0",
    "wrangler": "^4.131.2"
  },
  "dependencies": {
    "postal-mime": "^3.0.0"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "workerd@1.20260911.1": true
  }
}
