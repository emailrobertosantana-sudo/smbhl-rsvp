{
  "name": "smbhl-rsvp-tmp",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.0.0",
    "acorn": "^8.18.0",
    "vitest": "~4.1.0",
    "wrangler": "^4.131.2"
  },
  "dependencies": {
    "postal-mime": "^3.0.0"
  }
}
